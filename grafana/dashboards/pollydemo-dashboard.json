{
  "annotations": { "list": [] },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "iteration": 1625247600000,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "ops/s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "target": {},
      "targets": [
        { "expr": "rate(pollydemo_consumer_total_calls[1m])", "legendFormat": "calls/sec", "refId": "A" }
      ],
      "title": "Consumer Calls (rate)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "ops/s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "rate(pollydemo_retries_total[1m])", "legendFormat": "retries/sec", "refId": "A" }
      ],
      "title": "Retries Fired (rate)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(pollydemo_consumer_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95 (s)",
          "refId": "A"
        }
      ],
      "title": "Request Duration (p95)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "none" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "pollydemo_circuit_state", "legendFormat": "state (0=Closed,1=Open)", "refId": "A" }
      ],
      "title": "Circuit State (gauge)",
      "type": "stat"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": { "unit": "ops/s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "targets": [
        {
          "expr": "rate(pollydemo_producer_failures_total[1m])",
          "legendFormat": "producer failures/sec",
          "refId": "A"
        }
      ],
      "title": "Producer Failures (rate)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": { "unit": "ops/s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 6,
      "targets": [
        {
          "expr": "rate(http_requests_total{app=\"ConsumerApi\"}[5m])",
          "legendFormat": "{{status_class}}",
          "refId": "A"
        }
      ],
      "title": "ConsumerApi HTTP Requests by Status Class (rate)",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 27,
  "style": "dark",
  "tags": ["polly", "prometheus"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {
    "refresh_intervals": ["5s", "10s", "30s", "1m", "5m"],
    "time_options": ["5m", "15m", "1h", "6h", "12h", "24h", "2d", "7d", "30d"]
  },
  "timezone": "browser",
  "title": "PollyDemo - Prometheus",
  "uid": "pollydemo-observability",
  "version": 1
}